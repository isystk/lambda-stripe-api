{
  "name": "stripe-subscription-api",
  "version": "1.0.0",
  "description": "stripe-subscription-api",
  "author": "isystk",
  "license": "MIT",
  "scripts": {
    "start": "IS_LOCAL=true node dist/local-app",
    "build": "tsc",
    "test": "jest",
    "test:coverage": "jest --collect-coverage",
    "lint": "eslint --ext .ts .",
    "lintfix": "eslint --fix --ext .ts .",
    "format": "prettier -w 'src/**/*.ts'",
    "fix": "npm run format && npm run lint"
  },
  "dependencies": {
    "@stripe/stripe-js": "^1.52.1",
    "@types/stripe": "^8.0.417",
    "aws-sdk": "^2.1373.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "express-session": "^1.17.3",
    "lodash": "^4.17.21",
    "node-uuid": "^1.4.8",
    "nodemailer": "^6.9.1",
    "stripe": "^12.4.0"
  }
}
